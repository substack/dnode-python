# * Conn is an EE, at least in dnode-js

from scrub import scrub
import json

class Conn:
    def __init__(self, stream, wrapper):
        #This is where all encoding/decoding magic happens.
        pass
    def end(self):
        pass
